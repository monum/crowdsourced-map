import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  navContainer: {
    position: "fixed",
    bottom: 0,
    left: 0,
    right: 0,
    zIndex: 10,
    padding: "5px 0",
  },
}));
